import {gameActionTypes}from './games.types'
import GAME_DATA from './games.data'

const INITIAL_STATE = GAME_DATA;

const gamesReducer = (state=INITIAL_STATE, action) => {
    switch(action.type){
        
        
        
        default: return state;
    }
}